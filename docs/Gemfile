# Local preview only — GitHub Pages ignores this file and builds docs/ with its
# own pipeline. Run:  bundle install && bundle exec jekyll serve  (brew ruby).
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-redirect-from"
gem "webrick"   # required by `jekyll serve` on Ruby 3+
